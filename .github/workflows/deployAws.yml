name: EC2AWSDeployement

on: 
  push:
    branches:
      - main

jobs:
  AWSDeploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Server Deploy
        uses: easingthemes/ssh-deploy@main
        env:
          SSH_PRIVATE_KEY: ${{ secrets.EC2_SSH_KEY }}
          SOURCE: "./"
          REMOTE_HOST: "ec2-3-86-35-53.compute-1.amazonaws.com"
          REMOTE_USER: "t6-victorhugobs"
          REMOTE_PORT: "80"
          TARGET: "/home/t6-victorhugobs/"